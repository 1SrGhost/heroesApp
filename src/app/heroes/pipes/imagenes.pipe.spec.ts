import { ImagenesPipe } from './imagenes.pipe';

describe('Imagenes.PipePipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenesPipe();
    expect(pipe).toBeTruthy();
  });
});
